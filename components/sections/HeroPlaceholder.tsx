/**
 * Shell only. The real hero is the Spline "SF Robot Character" scene,
 * embedded via Copy Embed iframe in PLAN.md Step 4 — out of scope here.
 * This reserves the slot so the chapters below compose correctly.
 */
export default function HeroPlaceholder() {
  return (
    <section id="chapter-hero" className="hero-placeholder">
      <p className="hero-placeholder__note">
        [ hero: Spline &ldquo;SF Robot Character&rdquo; embed, Step 4 ]
      </p>
    </section>
  );
}
